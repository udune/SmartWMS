<Window x:Class="SmartWMS.Views.RegisterMaterialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Register New Material"
        Width="640" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Input TextBox Style -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="FormComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="12,0,30,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="Placeholder"
                                               Text="Select Material Type"
                                               Foreground="#94a3b8"
                                               Margin="12,0,30,0"
                                               VerticalAlignment="Center"
                                               Visibility="Collapsed"/>
                                    <Path x:Name="Arrow"
                                          Fill="#94a3b8"
                                          HorizontalAlignment="Right"
                                          Margin="0,0,12,0"
                                          VerticalAlignment="Center"
                                          Data="M0,0 L4,4 L8,0"/>
                                    <Popup x:Name="Popup"
                                           Placement="Bottom"
                                           IsOpen="{TemplateBinding IsDropDownOpen}"
                                           AllowsTransparency="True"
                                           Focusable="False"
                                           PopupAnimation="Slide">
                                        <Border x:Name="DropDownBorder"
                                                Background="White"
                                                BorderBrush="#e5e7eb"
                                                BorderThickness="1"
                                                CornerRadius="8"
                                                Margin="0,4,0,0"
                                                MinWidth="{TemplateBinding ActualWidth}"
                                                MaxHeight="200">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="8" Opacity="0.15" ShadowDepth="2"/>
                                            </Border.Effect>
                                            <ScrollViewer Margin="4">
                                                <StackPanel IsItemsHost="True"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Border>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="SelectedItem" Value="{x:Null}">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Item Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#f0f2f4"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#eff6ff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="FormDatePicker" TargetType="DatePicker">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFFFFF33"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Modal Container -->
    <Border Background="White" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" Opacity="0.25" ShadowDepth="8"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="12,12,0,0">
                <Grid Margin="24,16">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="20"
                                   Foreground="White" VerticalAlignment="Center"/>
                        <TextBlock Text="Register New Material" FontSize="18" FontWeight="Bold"
                                   Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Style="{StaticResource CloseButton}"
                            HorizontalAlignment="Right"
                            Click="CloseButton_Click">
                        <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Form Body -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="24,20">
                    <!-- Material Name -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Material Name" Style="{StaticResource FormLabel}"/>
                        <TextBox x:Name="MaterialNameTextBox"
                                 Style="{StaticResource FormTextBox}"/>
                        <TextBlock x:Name="MaterialNamePlaceholder"
                                   Text="e.g. Aluminum Casing - Type A"
                                   Foreground="#94a3b8" FontSize="14"
                                   Margin="14,-30,0,0" IsHitTestVisible="False"
                                   Visibility="Visible"/>
                    </StackPanel>

                    <!-- Vendor -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Vendor" Style="{StaticResource FormLabel}"/>
                        <Grid>
                            <TextBox x:Name="VendorTextBox"
                                     Style="{StaticResource FormTextBox}"
                                     Padding="40,0,12,0"/>
                            <TextBlock Text="&#xE8D1;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                       Foreground="#94a3b8" VerticalAlignment="Center"
                                       Margin="14,0,0,0" IsHitTestVisible="False"/>
                            <TextBlock x:Name="VendorPlaceholder"
                                       Text="Select or enter vendor name"
                                       Foreground="#94a3b8" FontSize="14"
                                       Margin="42,-1,0,0" VerticalAlignment="Center"
                                       IsHitTestVisible="False" Visibility="Visible"/>
                        </Grid>
                    </StackPanel>

                    <!-- Lot ID & Type -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Lot ID -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Lot ID" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="LotIdTextBox"
                                     Style="{StaticResource FormTextBox}"/>
                            <TextBlock x:Name="LotIdPlaceholder"
                                       Text="LOT-2023-001"
                                       Foreground="#94a3b8" FontSize="14"
                                       Margin="14,-30,0,0" IsHitTestVisible="False"
                                       Visibility="Visible"/>
                        </StackPanel>

                        <!-- Type -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Type" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="TypeComboBox"
                                      Style="{StaticResource FormComboBox}">
                                <ComboBoxItem Content="Raw Material"/>
                                <ComboBoxItem Content="Finished Good"/>
                                <ComboBoxItem Content="Packaging"/>
                                <ComboBoxItem Content="Electronic Component"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Quantity & Expiry Date -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Quantity -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Quantity (Units)" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="QuantityTextBox"
                                     Style="{StaticResource FormTextBox}"
                                     PreviewTextInput="QuantityTextBox_PreviewTextInput"/>
                            <TextBlock x:Name="QuantityPlaceholder"
                                       Text="0"
                                       Foreground="#94a3b8" FontSize="14"
                                       Margin="14,-30,0,0" IsHitTestVisible="False"
                                       Visibility="Visible"/>
                        </StackPanel>

                        <!-- Expiry Date -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Expiry Date" Style="{StaticResource FormLabel}"/>
                            <Grid>
                                <DatePicker x:Name="ExpiryDatePicker"
                                            Height="44"
                                            FontSize="14"
                                            VerticalContentAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#f8fafc"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,16">
                    <Button Style="{StaticResource SecondaryButton}"
                            Content="Cancel"
                            Width="100"
                            Click="CancelButton_Click"/>
                    <Button Style="{StaticResource PrimaryButton}"
                            Margin="12,0,0,0"
                            Click="RegisterButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="Register Material" Margin="8,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>